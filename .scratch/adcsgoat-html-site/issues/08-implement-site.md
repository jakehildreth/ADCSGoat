## Spec: ADCSGoat HTML Site

See the full PRD at [spec.md](../spec.md).

Status: ready-for-agent

## Summary

Implement the ADCSGoat HTML site as a plain-HTML/CSS/JS static bundle embedded in the `ADCSGoat` PowerShell module. The site teaches students the AD CS attack paths defined in the lab, plus setup, prerequisites, tools, and per-path remediation.
